<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Payhour Translation</title>
    <style>
      body {
        background-color: #efe9df;
        position: relative;
        overflow-y:scroll;
      }
      
      h1 {
        color:#262627;
        font-size: 22px;
        font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        font-weight: 700;
        margin-bottom: 64px;
        text-transform: uppercase;
        position:sticky;
        top: 16px;
        left: 16px;
      }

      p, textarea, label {
        font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        font-size: 18px;
        line-height: 20px;
      }

      ul{
        list-style-type: none;
        padding-left: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      li{
        background-color: #fcfaf7;
        border-radius: 8px;
        padding: 52px;
        margin-bottom: 64px;
        width: 60%;
        box-shadow: rgba(0, 0, 0, 0.16) 0px 0px 0px;
         -webkit-transition:  box-shadow .3s ease-out;
         box-shadow: rgba(0, 0, 0, 0.16) 0px 1px 4px;
      }

      li:hover {
        box-shadow: rgba(0, 0, 0, 0.1) 0px 4px 12px;
        -webkit-transition:  box-shadow .3s ease-in;
      }

      .description {
        font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        font-size: 24px;
        line-height: 24px;
      }

      .label, .englishLabel {
        font-size: 14px;
        line-height: 16px;
        margin: 1rem;
        margin-left: 0;
      }

      .language {
        font-size: 16px;
        line-height: 16px;
        width: 50%;
      }

      label {
        display: block;
      }

      textarea {
        width: 100%;
        padding: 8px;
      }

      .container {
        display: flex;
      }

      .buttonWrapper {
        position: -webkit-sticky; /* Safari */
        position: sticky;
        bottom: 16px;
        right: 16px;
        text-align: right;
      }

      button {
        padding: 16px;
        padding-left: 32px;
        padding-right: 32px;
        background-color: #262627;
      color: #efe9df;
      font-size: 18px;
      border-radius: 8px;
      cursor: pointer;
      }

      textarea::placeholder{
        overflow:visible;
      }
    </style>
  </head>
  <body>
    <h1>Payhour Backoffice ✏️</h1>
    <form action="" method="POST">
      <ul>
      <% data.forEach(function(item, index) { %>   
        <li>  
          <h3 class="description"><%= item[2] %></h3>
          <div class="container">
            <div class="language">
              <p class="label">Swedish:</p>
              <p><%= item[3] %></p>
            </div>
            <div class="language">
              <label class="englishLabel" for=<%= item[0] %>>English:</label>
              <textarea  rows="3" cols="42" class="englishInput" id=<%= item[0] %> name=<%= item[0] %> placeholder="<%- item[4] %>"  ></textarea>
            </div>
        </div>
        </li>   
        <% }); %>
      </ul>
      <div class="buttonWrapper">
      <button type="submit">Save</button>
    </div>
    </form>
  </body>
</html>
